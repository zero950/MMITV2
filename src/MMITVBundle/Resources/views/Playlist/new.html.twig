{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Playlist creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('playlist') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
